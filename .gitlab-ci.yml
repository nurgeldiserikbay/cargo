stages:
  - build

variables:
  DOCKER_IMAGE_NAME: tauekel

build:
  image: docker:latest
  stage: build
  interruptible: true
  only:
    - master
  services:
    - docker:dind
  before_script:
    - echo $CI_REGISTRY_PASSWORD | docker login $DOCKER_REGISTRY --username $CI_REGISTRY_USER --password-stdin
  script:
    — docker build -t tauekel .
    — docker tag tauekel thelightcome/tauekel:0.0.1
    — docker push thelightcome/tauekel:0.0.1